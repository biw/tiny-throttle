{
  "name": "tiny-throttle",
  "description": "🚗 A 311 byte (gziped) throttle & debounce package",
  "version": "1.0.1",
  "jsnext:main": "dist/tiny-throttle.es.js",
  "module": "dist/tiny-throttle.es.js",
  "main": "dist/tiny-throttle.js",
  "umd:main": "dist/tiny-throttle.umd.js",
  "repository": "https://github.com/719ben/tiny-throttle",
  "keywords": [
    "throttle",
    "debounce",
    "lodash"
  ],
  "author": "719Ben <ben.williams@colorado.edu>",
  "license": "MIT",
  "scripts": {
    "test": "node_modules/.bin/jest tests",
    "lint": "node_modules/.bin/eslint **/*.js --fix",
    "pack": "node_modules/.bin/webpack -p",
    "reinstall": "rm -r node_modules && yarn cache clean && yarn",
    "build": "npm-run-all --silent clean -p rollup -p minify:* -s docs size",
    "clean": "rimraf dist",
    "rollup": "rollup -c",
    "minify:cjs": "uglifyjs $npm_package_main -cm toplevel --source-map -o $npm_package_main",
    "minify:umd": "uglifyjs $npm_package_umd_main -cm -o $npm_package_umd_main"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.2.1",
    "babel-jest": "^22.2.2",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "eslint": "^4.17.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "jest": "^22.2.2",
    "npm-run-all": "^4.1.2",
    "prettier-eslint-cli": "^4.7.0",
    "rollup": "^0.55.5",
    "rollup-plugin-buble": "^0.19.2",
    "rollup-plugin-flow": "^1.1.1",
    "uglify-js": "^3.3.10"
  }
}
